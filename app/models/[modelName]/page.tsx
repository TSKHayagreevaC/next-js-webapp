import { Header } from "@/app/components/header"

export default function Page({ params }: { params: { slug: string } }) {
    return (
        <div>
            <h1>Model Detals Page</h1>
        </div>
    )
  }